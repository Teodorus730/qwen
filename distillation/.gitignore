# Large local HF cache (model weights + streamed dataset)
.hf_cache/
# Local FineWeb-Edu corpus cache (rebuild with src/fetch_corpus.py)
data/
# Saved student checkpoints can be large
results/**/student/
# Python
__pycache__/
*.pyc

# Transient run artifacts — regenerated on every run; keep locally.
results/**/*.log
results/**/live.png
